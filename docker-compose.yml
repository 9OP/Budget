services:
  budget:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPREADSHEET_ID=${SPREADSHEET_ID}
      - SERVICE_ACCOUNT_PATH=/run/secrets/sa.json
    volumes:
      - ./service-account.json:/run/secrets/sa.json:ro
